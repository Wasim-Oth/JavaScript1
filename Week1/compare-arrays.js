'use strict'
let food = ['pizza', 'shorma', 'kabab', 'salada' ];
let drinks = ['beer', 'cola', 'water', 'vodka', 'juice', 'wisky', 'soda'];

console.log('the length of the array is:' +' ' + food.length);
console.log('the length of the array is:' +' ' + drinks.length);

if (food.length == drinks.length){
console.log('They are the same!');
}
else{
    console.log('Two different sizes');
}